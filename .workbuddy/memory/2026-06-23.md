# 2026-06-23

## AGENTS.md 全面升级
- 完整扫描了后端 20 个 Controller、19 个 Service、32 个 Entity、34 个 Mapper、6 个 Config、1 个 Aspect、1 个 WebSocket
- 全面重写 AGENTS.md，从 224 行扩展到 400+ 行
- 新增内容：
  - 完整 Controller/Service/Config 目录树（之前只列了 controller/service/impl/entity/mapper/filter/handler/utils）
  - 32 张数据库表完整速查表（按模块分组：用户权限5、班级2、考试题库6、题库选项3、收藏2、讨论区5、PK对战5、学习小组4、系统1）
  - 完整路由表（含 discuss 子路由和通配路由注意事项）
  - 完整 API 参考（20 个控制器所有端点）
  - 特色功能模块（AI批改、智能推荐、证书生成、积分签到、系统消息、Excel导入、WebSocket PK、审计日志、敏感词过滤）
  - 新增 5 个高频陷阱（JWT解析方式、角色deleted过滤、Vite Proxy Authorization头、SecurityFilterChain securityMatcher、枚举getter序列化）
  - 修复模式 F（敏感词过滤）
  - 快速启动命令和测试账号表
  - 关键文件索引表

## 发布帖子500错误修复 (BugFix)
- 根因1：PostEditor.vue 第105行 `const post = res.data` 中 `res` 未声明（编辑帖子时100%报错）
- 根因2：DiscussionServiceImpl.createPost() 未设 contentType/isAuditPassed 默认值，某些配置下可能导致null插入
- 修复：`const res = await getPostDetail(id)` + 加 null 检查设默认值 1
- Commit: `bfe323d`
- QA回归测试：5/5 全部通过

## 试卷导入模板添加填写说明
- 修改 `backend/src/main/resources/import-template-exam.xlsx`，新增首个 Sheet「填写说明」
- 内容：模板结构概览、试卷信息Sheet字段说明、题目列表Sheet字段说明、注意事项、快速开始指南
- 用 Python openpyxl 3.1.5 实现，含表头样式、列宽、边框等格式化
- 同时同步到 target/classes/

## 题库练习分类点击无反应 Bug
- **根因**: root-level 分类（如高中数学, id=27）无子节点，模板只渲染为 tree-parent（@click=toggleExpand），无法触发 selectCategory→loadQuestions
- 后端/数据库均正常（allow_practice 列存在，65题全部 enable，高中数学有44题）
- **修复**:
  1. 前端 QuestionBank.vue: 无 children 的根分类渲染为 tree-child，点击触发 selectCategory
  2. loadQuestions() 增加 catch 兜底，选中前先清空旧列表
  3. onMounted 兼容叶子分类自动选中
  4. 数据库: UPDATE 13 个 categories allow_practice=1
  5. sql/add_practice_support.sql 迁移脚本 + schema.sql 更新
  6. AGENTS.md 新增陷阱#11
- Commit: `4af6afa`

## 教师数据隔离 + 加载卡死修复 (BugFix)
- 根因1: practiceTree() 未做教师隔离，学生看到所有19个分类而非本班老师的2个
- 根因2: loadQuestions() 串行 for...of+await 加载每题详情，44题×15s超时=11分钟卡死
- 修复: QuestionCategoryController 新增 getStudentTeacherIds()→wrapper.in(creatorId)；QuestionBank.vue 改 Promise.all 并行
- Commit: `b9e3ef5`
- QA: 6/6 PASS（场景1教师隔离/场景2加载速度/场景3向后兼容）

## 交付前全面代码审查
- 审查范围：21个Controller、16个ServiceImpl、26个Vue视图、32个Entity vs DB列
- 修复 P0: ExamPaperController.create() NPE风险 — duration/totalScore/passScore/paperType 增加null检查+默认值
- 修复 P1: SmsController.sendCode() — API响应中移除验证码明文泄露
- 已验证: all DB columns match entities, no recent SQL errors, all @PreAuthorize present
- 已知非阻塞: study_group_chat表无Entity类(聊天功能未实现)、SmsController内存存储(仅演示)
- Commit: `5c18a34`

## 讨论区管理入口权限 + 头像裁剪修复
- MainLayout.vue: 教师侧边栏移除/admin/discuss入口，仅超管可见
- router/index.ts: /admin/discuss meta requiresTeacher→requiresAdmin
- AvatarCropper.vue: 修复裁剪预览黑色 — crop-img 内联 transform 缺 -50% 居中偏移
- Commits: `37db9fe`, `ac6d23e`

## 学生 mark 题库练习无限加载修复 (BugFix)
- **用户报告**: 学生 mark（已入班"高三冲刺班"）点击题库分类后无限加载
- **深入排查**:
  1. DB验证: mark id=51, class_member (class_id=26, status=1, role=0), teacher_id=13, 4个分类有allow_practice=1且含题目
  2. 后端API测试: /question/category/practice-tree → 返回2个分类✅, /question/practice → 44题正常返回✅
  3. 前端代码审查: 正常路径无bug，但错误处理不够健壮
- **根因**: 当API因JWT过期/网络问题返回401/403时，onMounted缺少catch块导致错误吞没，loadQuestions的catch条件判断过严
- **修复**: QuestionBank.vue 两处变更:
  1. onMounted: 新增catch块清空categories（避免空白区域），显示"暂无可练习的题库"
  2. loadQuestions: 简化catch逻辑，始终清空questions+添加console.error日志
- 代码审查: ✅ loading状态始终正确关闭，模板在所有状态路径下正确渲染
